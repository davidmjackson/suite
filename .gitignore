node_modules/
.env
.env.local
.env.*.local
*.log
.DS_Store
dist/
build/
.vscode/
.idea/
*.swp
.claude/

# Brainstorming visual-companion artifacts
.superpowers/

# Design workspace — source-of-truth zips + extracted "Instrument" handoff,
# large assets/screenshots. Kept out of the repo; shippable CSS/tokens get
# integrated into each app's source tree during the redesign build.
project-design-docs/

# Architecture docs: keep the markdown, exclude the binary bundle
docs/architecture/apps.zip

# graphify knowledge-graph output (regenerable tool artifact + cache)
graphify-out/
