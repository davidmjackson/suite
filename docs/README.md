# Documentation

Architecture and operational documents for the Sprint Suite.

- **`architecture/`** — per-app architecture references (hub, raid, signal, poker, retro) for picking up the codebase cold.
- **`legal/`** — working drafts of the Free Use Licence and Data & Privacy Note (the live `/license` copy lives in `hub/views/license.eta`; `/privacy` publication is still pending). Interim, unreviewed — solicitor pass outstanding.

Completed superpowers specs/plans (shipped work) are kept in git history and archived outside the repo at `~/suite-archive/docs/superpowers/`. Design-workspace artifacts (`project-design-docs/`) and the architecture `apps.zip` bundle are likewise archived at `~/suite-archive/`. The earlier cancelled Clerk integration docs are at `~/clerk-archive/`.
