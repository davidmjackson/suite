{
  "name": "@suite/marketing",
  "version": "0.1.0",
  "private": true,
  "description": "sprintsuite.uk marketing surface — static pages, no runtime deps",
  "type": "module",
  "scripts": {
    "drift": "node /var/www/suite/shared/theme/check-theme-drift.mjs /var/www/suite/marketing",
    "test": "npm run drift && node --test tests/"
  },
  "engines": {
    "node": ">=20"
  }
}
